export enum userRoles {
    USER = 'user',
    ADMIN = 'admin',
    SUPER_ADMIN = 'super-admin',
}

export enum participantStatus {
    PENDING = 'pending',
    HAS_PICKED = 'has-picked',
    WAS_PICKED = 'was-picked',
    DONE = 'done'
}